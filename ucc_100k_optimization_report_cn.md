# UCC 10 万门结构化电路优化报告

## 1. 项目目标
`ucc`（Unitary Compiler Collection）的目标是为不同量子电路前端和目标后端提供统一的编译接口。

从实际实现来看，这个项目并不是要从零开始构建一套全新的底层编译器栈，而是把以下能力封装到一个统一接口中：
- 基于 qBraid 的跨框架电路转换，
- 基于 Qiskit 的编译和转译，
- 目标后端 / 目标门集约束，
- 以及可扩展的自定义 pass 机制。

简而言之，这个项目希望提供：
- 统一的编译入口，
- 默认优化流水线，
- 多前端兼容能力，
- backend / gateset 定向约束，
- 以及可插拔的优化 pass 架构。

## 2. 最初发现的问题
这个项目最初暴露出的主要问题，并不是简单的功能错误，而是默认编译策略在大规模结构化电路上的算法性缺陷。

核心问题有两点：
- 过早进行 basis lowering，破坏了高层结构信息；
- 默认流程主要依赖局部 rewrite / synthesis，缺乏足够的结构感知能力。

因此，编译器经常无法识别如下全局消除机会：
- `U` 后接 `U†`，
- 重复出现的结构化块，
- 可交换的逆操作链。

结果不是把结构化电路化简，而是把它们严重膨胀。

这个问题在 `QFT + inverse-QFT` 上最明显，但在大规模 ring-style 分层电路上也同样存在。

## 3. 最初的 10 万门测试结果
我们使用了三类精确为 `100,000` 门的结构化电路作为基准。

| 电路 | 输入门数 | 输出门数 | 输入深度 | 输出深度 | 输入双比特门数 | 输出双比特门数 | 编译时间 |
|---|---:|---:|---:|---:|---:|---:|---:|
| QFT + inverse-QFT | 100,000 | 1,049,611 | 33,324 | 239,924 | 83,300 | 146,612 | 6.78 s |
| QAOA-like Ring | 100,000 | 290,092 | 62,000 | 170,189 | 40,000 | 39,940 | 1.75 s |
| Basis-Friendly Ring | 100,000 | 290,321 | 62,000 | 170,425 | 40,000 | 39,940 | 1.68 s |

### 最初结果的含义
- `QFT + inverse-QFT` 出现了灾难性回退：一个本应高度可消除的电路被放大到了超过 `100 万` 门。
- 两类 ring 电路也都发生了严重退化：总门数和深度大幅上升，而双比特门数几乎没有改善。
- 这说明原始默认流水线更像一个局部 rewrite 系统，而不是面向大电路的优化器。

## 4. 优化过程
这次修复不是简单地接入一个更重的优化器，而是分阶段引入结构感知逻辑来改进算法。

### 第一阶段：反回退保护
首先引入了一个保守的结构代价模型，用于比较不同候选输出，并拒绝那些明显更差的结果。

这一步先解决了一个直接问题：
- 编译器至少不能把一个明显更差的电路直接当成结果返回。

不过，这一步本身并没有增加结构理解能力，它只是阻止灾难性回退。

### 第二阶段：结构预化简
然后在 basis lowering 之前增加了结构预化简步骤，这样高层消除机会可以在电路被激进分解前先被利用。

第一条有效规则是：
- `CommutativeInverseCancellation`

这一步很关键，因为很多消除机会在分解之后会变得很难再恢复。

### 第三阶段：重复块优化
接着加入了重复块复用：
- 检测重复的前缀块，
- 只优化一个块实例，
- 再把优化后的结果复用到所有重复位置。

这显著降低了重复结构电路上的重复计算成本。

### 第四阶段：更一般的块匹配
最后又加入了更一般的结构规则：
- 任意位置的重复块 run 检测，
- 相邻 inverse-block 的整块删除。

为了避免预处理本身成为瓶颈，最终实现被拆成快路径和通用路径：
- 对非常大的电路，优先走前缀快路径；
- 较重的通用块扫描只在中等规模电路上开启。

## 5. 最终的 10 万门测试结果
在加入结构优化层之后，我们重新运行了同样的 `100,000` 门基准。

| 电路 | 输入门数 | 输出门数 | 输入深度 | 输出深度 | 输入双比特门数 | 输出双比特门数 | 编译时间 |
|---|---:|---:|---:|---:|---:|---:|---:|
| QFT + inverse-QFT | 100,000 | 0 | 33,324 | 0 | 83,300 | 0 | 0.45 s |
| QAOA-like Ring | 100,000 | 99,858 | 62,000 | 61,933 | 40,000 | 39,940 | 5.21 s |
| Basis-Friendly Ring | 100,000 | 79,960 | 62,000 | 60,940 | 40,000 | 39,940 | 5.51 s |

## 6. 结果对比
和最初版本相比，当前编译器已经修复了结构化 `10 万门` 工作负载上的主要算法性回退问题。

关键变化如下：

### QFT + inverse-QFT
- 初始版本：`100,000 -> 1,049,611`
- 最终版本：`100,000 -> 0`
- 编译时间也从 `6.78s` 降到了 `0.45s`

这说明编译器已经不再破坏这种结构，而是可以直接识别并将其完全抵消。

### QAOA-like Ring
- 初始版本：膨胀到 `290,092` 门
- 最终版本：收敛到 `99,858` 门

这说明默认流水线已经不会再破坏这类结构化分层电路，输出规模基本保持在原始量级附近。

### Basis-Friendly Ring
- 初始版本：膨胀到 `290,321` 门
- 最终版本：压缩到 `79,960` 门

这表明当前优化器不仅能避免回退，而且在输入本来就接近目标 basis 时，还能产生实际压缩收益。

## 7. 验证结果
在完成优化后，重新运行了完整测试集。

- 最终测试结果：`118 passed, 10 warnings`

这些 warning 主要来自若干可选依赖未安装（例如 `qmprs`、`kahypar`、`optuna/cmaes/nevergrad`），并不是功能性错误。

## 8. 结论
这个项目最初的核心问题，不是它无法处理大规模电路，而是默认优化策略在结构上过于朴素，因此在结构化工作负载上容易出现严重膨胀。

通过加入：
- 结构预化简，
- 重复块复用，
- inverse-block 消除，
- 以及快路径与通用结构扫描的分层策略，

当前版本已经把默认流水线从一个容易回退的局部 rewrite 流程，改造成了一个更适合大规模结构化电路的优化器。
